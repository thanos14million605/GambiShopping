const createProduct = (newProduct) => {
  console.log(newProduct);
};

const fetchAllProducts = () => {};

const fetchProduct = (id) => {
  console.log(id);
};

const updateProduct = (id, newProductData) => {
  console.log(id, newProductData);
};

const deleteProduct = (id) => {
  console.log(id);
};

export default {
  createProduct,
  fetchAllProducts,
  fetchProduct,
  updateProduct,
  deleteProduct,
};
